//Sets are unordered collections of elements.
//Sets sets guarantee uniqueness of elements, the duplicate is discarded in the set created:

val fruits: Set<String> = setOf("Apple","Banana", "Apple")
println(fruits)
println(fruits::class)
println(fruits::javaClass)
